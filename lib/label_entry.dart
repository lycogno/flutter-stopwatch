import 'package:flutter/material.dart';
/*
class LabelEntry extends StatelessWidget {
  const LabelEntry({super.key});
  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 70.0,
      width: 100.0,
      child: TextField(
        decoration: InputDecoration(
          hintText: label == '' ? 'Stopwatch ${i + 1}' : label,
        ),
        onChanged: ((value) => onChangedTextCallback(value)),
      ),
    );
  }
}
*/